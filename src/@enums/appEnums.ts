export enum PlayerStatus {
  IDLE = '',
  NOW_PLAYING = 'Now Playing',
  PAUSED = 'Paused',
}

export enum LoadingStates {
  IDLE = 'IDLE',
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',
  ERROR = 'ERROR',
}
